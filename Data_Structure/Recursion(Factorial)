#include <stdio.h>

int factorial(int n)
{
	if (n == 1)
		return 1;
	else
		return (n * factorial(n - 1));
}

int main()
{
	int number = 0;
	printf("Input Number : ");
	scanf_s("%d", &number);
	number = factorial(number);
	printf("Factorial Result : %d\n", number);
}
