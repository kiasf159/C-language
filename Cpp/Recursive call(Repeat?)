#include <iostream>
using namespace std;

int sub(int x)
{
	if (x == 0)
	{
		return 0;
	}
	cout << "x = " << x << "\n";
	return(sub(x - 1));
}

int main()
{
	int x;
	cout << "Input Number : ";
	cin >> x;
	sub(x);
}
